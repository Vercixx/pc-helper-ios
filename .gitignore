# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/

# Node / Expo
node_modules/
.expo/
dist/
web-build/
*.tsbuildinfo

# Native projects generated by `expo prebuild`
mobile/ios/
mobile/android/

# Local credentials -- never commit these
mobile/credentials.json
mobile/*.p8
mobile/*.p12
mobile/*.mobileprovision
mobile/*.ipa

# Runtime state. The server key lives here; committing it would hand over the
# PC's identity to anyone with repo access.
pc-service/state/
*.pem
refclient.json

.DS_Store
